# Fixtures directory for initial data

Place JSON fixture files here that can be loaded with:
python manage.py loaddata fixtures/<filename.json>

Note: This project primarily uses management commands for seeding data.